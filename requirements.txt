# Pinned with upper bounds on purpose. The project's central claim is that the
# backtest reproduces exactly; unpinned majors quietly break that. Bump
# deliberately and re-run `python3 src/run_backtest.py` when you do.
pandas>=2.2,<3.0
numpy>=1.26,<3.0
prophet>=1.1.5,<2.0
langgraph>=0.2,<0.7
langchain-core>=0.3,<0.4
langchain-community>=0.2,<1.0
langsmith>=0.1,<1.0
pydantic>=2.7,<3.0
streamlit>=1.38,<2.0
pytest>=8.2,<9.0

# LLM providers — install at least one.
# Ollama is local-only: it is NOT reachable from a deployed container, so a
# hosted API is required for anything beyond running on your own machine.
# langchain-ollama>=0.2,<0.4        # local dev; needs `ollama serve` on the same host
langchain-groq>=0.2,<0.4        # GROQ_API_KEY   — fastest, generous free tier
# langchain-google-genai>=2.0,<3  # GOOGLE_API_KEY — free tier
# langchain-openai>=0.2,<0.4      # OPENAI_API_KEY — paid

# optional — only needed if the WhatsApp channel is enabled
# twilio>=9.0,<10.0
